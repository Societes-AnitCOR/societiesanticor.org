<nav class="navbar navbar-dark navbar-expand-sm bg-noir">
    <a class="navbar-brand" href="#">
        <img src="{{ asset('img/logo.svg') }}" class="mylogo">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto center-block">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Tous le monde peut aider !
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="#">Action</a>
            <a class="dropdown-item" href="#">Another action</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Something else here</a>
            </div>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('companies') }}">
            Entreprise engagées
            </a>
        </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
        <div class="btn-group ">
            <a href="{{ path('company_register') }}" class="btn btn-info participer">Je veux participer</a>
        </div>
        <div class="btn-group">
            {% if app.user %}
                <a href="#" class="btn btn-default btn-connexion">{{ app.user.username }}</a>
                <a href="{{ path('app_logout') }}" class="btn btn-default btn-connexion">Déconnexion</a>
                {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-default btn-connexion">Connexion</a>
            {% endif %}
            
        </div>
        </form>
    </div>
</nav>